/**
 * Component-specific notes for RegistrySlots.
 * Keyed by ComponentEntry.name (PascalCase).
 */
export const SLOT_NOTES: Record<string, string[]> = {
  Table: ['TableRow is also valid inside TableHeader.'],
};
